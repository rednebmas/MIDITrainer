import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("Settings placeholder")
                .navigationTitle("Settings")
        }
    }
}
